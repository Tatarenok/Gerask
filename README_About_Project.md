### 📂 Корневая директория (ticket-system/)

Содержит только основные мета-файлы проекта и две главные папки с кодом.

- backend/ — серверная часть приложения.
- frontend/ — клиентская часть приложения.
- logs/ — папка для логов (содержит .gitkeep , пустая).
- README.md — файл с документацией.
- LICENSE — файл лицензии.
- .gitattributes — настройки Git.

### 🐍 Backend (backend/)

Реализован на Python с использованием фреймворка FastAPI .
Структура выглядит следующим образом:

- app/ — Основной исходный код приложения:
  - main.py — Точка входа в приложение (запуск сервера).
  - config.py — Конфигурация приложения.
  - database.py — Настройка подключения к базе данных (PostgreSQL).
  - models/ — ORM-модели базы данных (файлы user.py , ticket.py , comment.py , role.py ).
  - schemas/ — Pydantic-схемы для валидации данных API (те же сущности: user, ticket, comment).
  - routers/ — Маршрутизаторы (endpoints) API ( auth.py , users.py , tickets.py , comments.py ).
  - services/ — Слой бизнес-логики ( auth_service.py , ticket_service.py ).
  - utils/ — Вспомогательные утилиты ( logger.py , security.py ).
  - middleware/ — Промежуточное ПО (например, logging.py ).
- alembic/ — Система миграций базы данных (содержит папку versions/ с файлами миграций).
- logs/ — Логи работы бэкенда ( app.log ).
- uploads/ — Папка для загруженных пользователями файлов (изображения).
- requirements.txt — Список Python-зависимостей.
- .env — Файл переменных окружения (конфигурация).
- venv/ — Виртуальное окружение Python (содержит библиотеки).

### 🎨 Frontend (frontend/)

Реализован на Vue 3 с использованием сборщика Vite .
Структура стандартная для Vue-проекта:

- src/ — Исходный код фронтенда:
  - main.js — Точка входа приложения.
  - App.vue — Корневой компонент.
  - api/ — Модули для взаимодействия с бэкендом ( index.js ).
  - views/ — Страницы приложения ( HomeView.vue , LoginView.vue ).
  - components/ — Переиспользуемые UI-компоненты ( HelloWorld.vue ).
  - router/ — Настройка маршрутизации ( index.js ).
  - stores/ — Управление состоянием (Pinia) ( auth.js , tickets.js ).
  - assets/ — Статические ресурсы (логотипы, стили).
  - style.css — Глобальные стили.
- public/ — Публичные статические файлы ( vite.svg ).
- vite.config.js — Конфигурационный файл сборщика Vite.
- package.json и package-lock.json — Управление зависимостями и скриптами запуска (npm).
- .vscode/ — Настройки для редактора VS Code ( extensions.json ).

### Итог

Проект состоит из классической связки FastAPI + Vue 3 . Инфраструктурные файлы (Docker, Nginx, CI/CD скрипты) в данный момент в явном виде в структуре файлов отсутствуют (кроме стандартных конфигов фреймворков).

ticket-system/ # Твоя текущая структура
├── backend/ # ✅ Есть
├── frontend/ # ✅ Есть
├── logs/ # ✅ Есть
├── README.md # ✅ Есть
├── LICENSE # ✅ Есть
│
│ # ═══════ ДОБАВЛЯЕМ ═══════
│
├── .github/
│ └── workflows/
│ └── deploy.yml # CI/CD пайплайн
│
├── infrastructure/ # Вся DevOps конфигурация
│ ├── docker/
│ │ ├── backend.Dockerfile
│ │ └── frontend.Dockerfile
│ ├── docker-compose.yml # Базовый
│ ├── docker-compose.dev.yml # Для разработки
│ ├── docker-compose.prod.yml # Для прода
│ └── traefik/
│ └── traefik.yml
│
├── scripts/
│ ├── backup.sh
│ └── deploy.sh
│
├── .env.example # Пример переменных
├── Makefile # Удобные команды
└── .dockerignore # Что не копировать в образы
